"""harness — the Crucible orchestration package (Phase 1).

The harness is the product. Teacher, games, Players are adapters around the
frozen contracts in `contracts.py`. Phase 1 wires the whole pipeline end-to-end
with FAKES for Teacher / GameAdapter / PlayerTrainer plus the ONE real reward.
"""
