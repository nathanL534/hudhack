# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtualenvs
venv/
.venv/

# Generated artifacts
*.png
training/rft_log.json
results/
# Dry-loop sidecar cache + per-run artifacts (regenerated every run)
output/dry_loop/

# Claude Code agent worktrees (embedded git repos) + machine-local state — never commit
.claude/
